🃏 AZI Probability Calculator

Python-инструмент для точного расчёта вероятности победы в карточной игре «Ази» при неполной информации о картах оппонента. Используется полный перебор (brute-force) всех допустимых комбинаций карт противника и детерминированная симуляция игровых состояний.

ВОЗМОЖНОСТИ

1)Полный перебор всех возможных раскладов карт оппонента

2)Корректная реализация правил «Ази»: обязательный ход в масть, перебивание козырем, определение победителя взятки

3)Анализ стратегий первого хода: strongest, weakest, middle

ТЕХНОЛОГИИ

Python 3.x
Стандартная библиотека: itertools, random (только для тестов)

Структура проекта
azi/
├── constants.py    # колода и ранги
├── core.py         # правила и сравнение карт
├── strategies.py   # стратегии первого хода
├── probability.py  # расчёт вероятностей
├─ example.py      # пример использования

ПРИМЕР ИСПОЛЬЗОВАНИЯ
from azi import overall_probability_towin

my_cards = [('9', 'S'), ('Q', 'D'), ('6', 'H')]
trump = ('A', 'S')

probability = overall_probability_towin(
    my_cards=my_cards,
    trump=trump,
    first_move=None,
    my_first_turn=False
)

print(probability)

АЛГОРИТМ

1)Из оставшейся колоды генерируются все комбинации по 3 карты для оппонента
2)Для каждой комбинации моделируются все допустимые ходы в 3 взятках
3)Подсчитываются сценарии, где игрок выигрывает 2 или 3 взятки
4)Вероятность = число выигрышных сценариев / общее число сценариев

ОГРАНИЧЕНИЯ

1)Только игра 1 на 1
2)Экспоненциальная вычислительная сложность (brute-force)
3)Отсутствие стохастики и ошибок игроков

КАК МОЖНО УЛУЧШИТЬ

1)Монте-Карло для масштабирования
2)Unit-тесты (pytest)
3)Переход к OOP (Card, Hand, Game)
4)Web-интерфейс (Streamlit)

Автор: GoldenGlove
