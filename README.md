🃏 AZI Probability Calculator

Python-инструмент для точного расчёта вероятности победы в карточной игре «Ази» при неполной информации о картах оппонента. Используется полный перебор (brute-force) всех допустимых комбинаций карт противника и детерминированная симуляция игровых состояний.

Возможности

Полный перебор всех возможных раскладов карт оппонента

Корректная реализация правил «Ази»: обязательный ход в масть, перебивание козырем, определение победителя взятки

Анализ стратегий первого хода: strongest, weakest, middle

Детерминированный результат (без случайности)

Технологии

Python 3.x

Стандартная библиотека: itertools, random (только для тестов)

Структура проекта
azi/
├── constants.py    # колода и ранги
├── core.py         # правила и сравнение карт
├── strategies.py   # стратегии первого хода
├── probability.py  # расчёт вероятностей
├── example.py      # пример использования

Пример использования
from azi import overall_probability_towin

my_cards = [('9', 'S'), ('Q', 'D'), ('6', 'H')]
trump = ('A', 'S')

probability = overall_probability_towin(
    my_cards=my_cards,
    trump=trump,
    first_move=None,
    my_first_turn=False
)

print(probability)

Алгоритм

Из оставшейся колоды генерируются все комбинации по 3 карты для оппонента

Для каждой комбинации моделируются все допустимые ходы в 3 взятках

Подсчитываются сценарии, где игрок выигрывает 2 или 3 взятки

Вероятность = число выигрышных сценариев / общее число сценариев

Ограничения

Только игра 1 на 1

Экспоненциальная вычислительная сложность (brute-force)

Отсутствие стохастики и ошибок игроков

Roadmap

Монте-Карло для масштабирования

Unit-тесты (pytest)

Переход к OOP (Card, Hand, Game)

Web-интерфейс (Streamlit)

Автор: GoldenGlove
